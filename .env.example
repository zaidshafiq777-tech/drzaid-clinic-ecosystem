# Dr. Zaid Healthcare OS — secret NAMES only, never real values.
# Real secrets live ONLY in n8n Credentials/environment, never here, never in Git.
GEMINI_API_KEY=
GROQ_API_KEY=
AI_PRIMARY_PROVIDER=gemini
AI_FALLBACK_PROVIDER=groq
